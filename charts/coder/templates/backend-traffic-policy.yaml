apiVersion: gateway.envoyproxy.io/v1alpha1
kind: BackendTrafficPolicy
metadata:
  name: coder-http-upgrade
  namespace: coder
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: coder-homelab
  httpUpgrade:
    - type: "derp"
    - type: "websocket"
