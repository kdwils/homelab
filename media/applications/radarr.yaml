apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: radarr
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: radarr:homelab
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: media
    server: "https://kubernetes.default.svc"
  source:
    path: media/radarr/environments
    repoURL: "https://github.com/kdwils/homelab"
    targetRevision: media/radarr/staging
  sources: []
  project: media
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
