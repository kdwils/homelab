apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prowlarr
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: prowlarr:homelab
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: media
    server: "https://kubernetes.default.svc"
  source:
    path: media/prowlarr
    repoURL: "https://github.com/kdwils/homelab"
    targetRevision: homelab
  sources: []
  project: media
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
