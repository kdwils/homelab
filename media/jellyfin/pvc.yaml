apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
  namespace: media
  annotations:
    longhorn.io/num-replicas: "1"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
