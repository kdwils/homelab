apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/ingress.class: nginx
  name: vaultwarden
  namespace: vaultwarden
spec:
  rules:
    - host: vaultwarden.int.kyledev.co
      http:
        paths:
          - backend:  
              service:
                name: vaultwarden
                port:
                  name: http
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - vaultwarden.int.kyledev.co
      secretName: wildcard-kyledev-tls-prod
