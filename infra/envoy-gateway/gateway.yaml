apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: homelab
  namespace: envoy-gateway-system
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  gatewayClassName: homelab-gateway-class
  listeners:
    - name: https
      protocol: HTTPS
      port: 443
      hostname: "*.int.kyledev.co"
      tls:
        mode: Terminate
        certificateRefs:
          - name: kyledev-int-tls
            kind: Secret
            group: ""
