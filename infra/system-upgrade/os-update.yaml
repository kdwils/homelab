apiVersion: v1
kind: Secret
metadata:
  name: os-update
  namespace: system-upgrade
type: Opaque
stringData:
  upgrade.sh: |
    #!/bin/sh
    set -e
    secrets=$(dirname $0)
    apt-get update
    apt-get --assume-yes install curl libcurl4 libssl3 openssl
    apt-get -y upgrade
