apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sealedsecrets
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: kube-system:homelab
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: kube-system
    server: "https://kubernetes.default.svc"
  source:
    path: infra/sealedsecrets/environments
    repoURL: "https://github.com/kdwils/homelab"
    targetRevision: infra/sealedsecrets/homelab
  sources: []
  project: infra
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
