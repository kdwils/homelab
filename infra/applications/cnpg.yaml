apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cnpg
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: cnpg:homelab
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: cnpg-system
    server: "https://kubernetes.default.svc"
  source:
    path: infra/cnpg
    repoURL: "https://github.com/kdwils/homelab"
    targetRevision: homelab
  sources: []
  project: infra
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
