apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mealie
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: mealie:homelab
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: mealie
    server: "https://kubernetes.default.svc"
  source:
    path: apps/mealie/environments
    repoURL: "https://github.com/kdwils/homelab"
    targetRevision: apps/mealie/staging
  sources: []
  project: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
