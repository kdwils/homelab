apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pocketid
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: pocketid:homelab
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: pocketid
    server: "https://kubernetes.default.svc"
  source:
    path: apps/pocketid
    repoURL: "https://github.com/kdwils/homelab"
    targetRevision: homelab
  sources: []
  project: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
